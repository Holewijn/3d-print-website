// Add this single field to the Quote model in prisma/schema.prisma:
//
// model Quote {
//   ... existing fields ...
//   customerNote      String?     ← ADD THIS LINE (anywhere in the model is fine)
//   ... rest of fields ...
// }
//
// Then run: pnpm prisma db push && pnpm prisma generate
